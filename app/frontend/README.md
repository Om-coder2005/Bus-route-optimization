# Frontend scaffold

This is a starter React + Vite frontend scaffold for the Dubai bus optimization dashboard.

## Run locally

1. `cd app/frontend`
2. `npm install`
3. `npm run dev`

The current scaffold intentionally stays minimal while backend/data modules are being finalized.
